---
- hosts: localhost
  become: yes
  vars:
  # This encrypted password is generated by " python -c 'import crypt; print crypt.crypt("kiranpasso", "$1$SomeSalt$")' "
  # the password is "kiranpasso"
    password: $1$SomeSalt$POMrr1BzGwkdx26sxKuOW0
  tasks:
    - name: creting a user with encrypted password
      user:
        name: tester
        password: "{{ password }}" 
  
